# 2026-04-13 — dev-nexus Terraform & WIF Fix Session

## Status: Active work — pre-compaction flush

## Test Fixes (committed & pushed)
- Down to **9 failed, 280 passed** (from original 65 failed)
- 6 remaining: `test_complexity_workflow.py` (integration, needs async DB mock)
- 2: `test_containerization_readiness.py` (collection-order issue, pass individually)
- 1: `test_component_dependencies.py` (mock returns None)

## Terraform/WIF: In Progress

### Key Discovery: Terraform State is Out of Sync
Resources exist in GCP but Terraform state doesn't track them. Result: 409 conflicts on every apply.

### Critical Bug Found: Secret Import Script Wrong
Workflow imports ALL secrets with `[0]` but only 4 secrets use `count = 1` in terraform config:
- `google_secret_manager_secret_version.langsmith_api_key[0]` → has count ✓
- `google_secret_manager_secret_version.cloudrun_service_account[0]` → has count ✓
- `google_secret_manager_secret_version.allowed_origins[0]` → has count ✓
- `google_secret_manager_secret_version.allowed_origins_staging[0]` → has count ✓
- All other secrets (github_token, github_client_id, github_client_secret, etc.) → **NO count, so `[0]` import silently fails**

Fix: Only import indexed secrets with `[0]`. Non-indexed secrets import without index.

### WIF Provider Soft-Deleted
- `github-provider` state=DELETED, expireTime=2026-05-12 — can't recreate
- Solution: Rename to `github-provider-daroja` in terraform config (done, committed)
- WIF pool `github-pool` still exists and is valid — no changes needed there

### Changes Made (committed):
1. `terraform/github_actions_wif.tf` → `github-provider` → `github-provider-daroja`
2. `terraform/outputs.tf` → updated provider output
3. `terraform-wif-only/main.tf` → provider name updated
4. `terraform/prod.tfvars` → `github_actions_scope = "organization"`, `github_org = "DarojaAI"`
5. `.github/workflows/terraform-apply.yml` → WIF-only import uses new provider name

### Next Steps (blocked on user):
1. Run Cloud Shell: import WIF-only with new provider name
2. Fix secret import script: only use `[0]` for indexed secrets
3. Full terraform import of existing GCP resources

### GCP Context
- Project: `globalbiting-dev` (665374072631)
- WIF Pool: `github-pool` (exists, valid)
- Old WIF Provider: `github-provider` (soft-deleted, expire 2026-05-12)
- New WIF Provider: `github-provider-daroja` (to be created)
- Deploy SA: `github-actions-deploy@globalbiting-dev.iam.gserviceaccount.com` (exists)

### Files Modified This Session
- `.github/workflows/terraform-apply.yml` — WIF provider name + secret import fix (pending)
- `terraform/github_actions_wif.tf` — provider rename (committed)
- `terraform/outputs.tf` — provider output (committed)
- `terraform-wif-only/main.tf` — provider rename (committed)
- `terraform/prod.tfvars` — org scope (committed)
# 2026-04-13 — Session continued (post-compaction flush)

## OpenAI Provider Error Investigation

### What's Known
- Error: "Missing API key for provider 'openai'. Configure the gateway auth for that provider, then try again."
- Source: OpenClaw's `provider-api-key-auth-dVvNnCb0.js` — throws when something tries to authenticate with `openai` provider
- Only triggers in `#test-agent` — NOT in `#linux-desktop-seed` or other channels
- NOT from conversation messages — appears even with "hello?" messages
- The bot is NOT active in `#linux-desktop-seed` (not in channels allowlist, only 14 channels in test-agent guild)

### Root Cause (Probable)
The `@claude_code_integration` Discord component (part of `@mariozechner/pi-ai` package) starts when the Discord provider initializes. `pi-ai` uses OpenAI by default for coding agent features. Every message in an active Discord channel triggers this component to initialize, which hits the missing OpenAI API key.

### Key Clues
- All channels have identical `{}` config — difference is bot activity (test-agent active, linux-desktop-seed not configured)
- `#linux-desktop-seed` doesn't appear anywhere in OpenClaw config — bot only processes test-agent's 14 channels
- Error is a runtime warning from provider auth module, not from user messages
- No `openai` provider in auth.profiles — only `openrouter`

### Fix Options (not yet applied)
1. Set `OPENAI_API_KEY` env var (even dummy value would suppress error)
2. Add `openai` auth profile with real key
3. Investigate if `@claude_code_integration` can be disabled or reconfigured to use openrouter instead
4. Investigate why it only fires in test-agent (likely because that's the only active channel)

### Config References
- Discord guild: `1485047825967480862` (14 channels, only test-agent triggers it)
- Guilds in config: `globalbitings` (1485047825967480862) and `openclaw` (893792852216662118)
- openclaw guild has 18 channels — potentially also active if bot is in those
- Provider: `openrouter` only, no `openai` profile configured

### Follow-up
User asked to investigate what specifically triggers the openai call in the Discord message processing pipeline. Need to trace through Discord extension code. Also: user has NOT set up Claude Code integration deliberately — it's a default part of pi-ai which is a dependency of something in OpenClaw.

---
## Session Metadata
- Compaction at: ~19:18 UTC
- Working in: #test-agent Discord channel
- Last topic: Investigating openai provider error