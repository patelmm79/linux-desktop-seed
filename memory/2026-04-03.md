# Session Memory — 2026-04-03

## dev-nexus Backend — Full Resolution Session (10:38–14:35 UTC)

### Problem 1: Cloud Run service was 403 Forbidden
User wanted to make `https://pattern-discovery-agent-75l7mntama-uc.a.run.app` public.

### Root Cause: Terraform State Out of Sync
GCS Terraform state (`gs://globalbiting-dev-terraform-state/dev-nexus/prod/`) is severely out of sync with actual GCP resources. All postgres, WIF, and Secret Manager resources already exist in GCP but are missing or mis-addressed in Terraform state. A full `terraform apply` fails with 409 conflicts.

### Fixes Applied

**IAM fix (resolved at 11:21):** Replaced Terraform apply in `terraform-apply.yml` with direct `gcloud run services add-iam-policy-binding` — bypassed broken state. `allUsers` + `roles/run.invoker` now applied. Service went from 403 → 500.

**Deploy workflow broken — 0 jobs:** `deploy-production.yml` had two YAML errors:
1. `environment: prod` at job level inside `env:` block — invalid YAML key
2. Duplicate `TF_VAR_knowledge_base_repo` in env block
Fixed both.

**Deploy workflow — state lock:** Added force-unlock before terraform apply.

**Deploy workflow — variable validation errors:** `jwt_secret` empty (secret not set), `postgres_machine_type` was `n2-standard-2` (not in allowed e2-micro/small/medium list). Fixed machine_type to `e2-medium`.

**Deploy workflow — environment-scoped secrets:** Secrets set in GitHub prod environment, but `deploy` job didn't have `environment: prod` key. Added it.

**Deploy workflow — terraform apply 409 conflicts:** Even with correct secrets, `terraform apply` still tries to create GCP resources that already exist. Replaced Terraform deploy step with direct `gcloud run deploy` using `--set-secrets` with correct `dev-nexus-prod_` prefix. Terraform state reconciliation is deferred.

**Deploy workflow — cannot mix env vars and secrets:** Cloud Run already had secrets set via Secret Manager refs. Tried to use `--set-env-vars` → error "Cannot update environment variable to string literal". Switched to `--set-secrets` with correct `dev-nexus-prod_` prefix.

**Deploy workflow — deployments permission:** Added `permissions: deployments: write` to fix "Resource not accessible by integration" on GitHub deployment API.

**PostgreSQL not connecting:** The app was using SQLite because `USE_POSTGRESQL=true` and `POSTGRES_HOST/DB/USER` secrets weren't passed to the container. Added to deploy workflow:
- `--set-env-vars="USE_POSTGRESQL=true"`
- `--set-secrets="POSTGRES_HOST=dev-nexus-prod_POSTGRES_HOST:latest,POSTGRES_DB=dev-nexus-prod_POSTGRES_DB:latest,POSTGRES_USER=dev-nexus-prod_POSTGRES_USER:latest"`

### Final State
- ✅ Service public: `https://pattern-discovery-agent-75l7mntama-uc.a.run.app` — 200 OK
- ✅ 89 skills registered, version 2.0.0
- ✅ Database: PostgreSQL — healthy (previously SQLite/disabled)
- ✅ Deploy workflow: gcloud-based deploy works end-to-end
- ⚠️ Terraform state: still out of sync (deferred — separate session)

### Commits Pushed
- `db83a79` fix(ci): clean orphaned state before import to prevent 409 conflicts
- `4826062` fix(ci): set ENV explicitly in scripts, fix empty inputs.environment on push
- `f0c89c7` fix(ci): use gcloud directly for Cloud Run IAM — bypass broken state
- `e39e249` fix(ci): remove invalid 'environment:' job key and duplicate TF_VAR
- `0f2a4c6` fix(ci): force-unlock stale terraform state lock before apply
- `d07f24d` fix(ci): add environment:prod to deploy job to access environment-scoped secrets
- `aa63315` fix(ci): replace Terraform deploy with direct gcloud run deploy
- `40f43d7` fix(ci): use --set-secrets with correct dev-nexus-prod_ prefix
- `56c3d75` fix(ci): add deployments:write permission for GitHub deployment API
- `6e9ae0d` fix(ci): add USE_POSTGRESQL and postgres connection secrets to deploy

### Outstanding Issues (for future sessions)
1. **Terraform state reconciliation** — full `terraform apply` still broken due to state/GCP mismatch
2. **No UI on backend URL** — this is an A2A agent API server, not a web app. The frontend is the separate dev-nexus-frontend Vercel app.

---

## Previous Session (02:00–02:35 UTC) — elastica project

### Problem 1: Cloud Run service was 403 Forbidden
User wanted to make `https://pattern-discovery-agent-75l7mntama-uc.a.run.app` public.

### Root Cause: Terraform State Out of Sync
GCS Terraform state (`gs://globalbiting-dev-terraform-state/dev-nexus/prod/`) is severely out of sync with actual GCP resources. All postgres, WIF, and Secret Manager resources already exist in GCP but are missing or mis-addressed in Terraform state. A full `terraform apply` fails with 409 conflicts.

### Fixes Applied

**IAM fix (resolved at 11:21):** Replaced Terraform apply in `terraform-apply.yml` with direct `gcloud run services add-iam-policy-binding` — bypassed broken state. `allUsers` + `roles/run.invoker` now applied. Service went from 403 → 500.

**Deploy workflow broken — 0 jobs:** `deploy-production.yml` had two YAML errors:
1. `environment: prod` at job level inside `env:` block — invalid YAML key
2. Duplicate `TF_VAR_knowledge_base_repo` in env block
Fixed both.

**Deploy workflow — state lock:** Added force-unlock before terraform apply.

**Deploy workflow — variable validation errors:** `jwt_secret` empty (secret not set), `postgres_machine_type` was `n2-standard-2` (not in allowed e2-micro/small/medium list). Fixed machine_type to `e2-medium`.

**Deploy workflow — environment-scoped secrets:** Secrets set in GitHub prod environment, but `deploy` job didn't have `environment: prod` key. Added it.

**Deploy workflow — terraform apply 409 conflicts:** Even with correct secrets, `terraform apply` still tries to create GCP resources that already exist. Replaced Terraform deploy step with direct `gcloud run deploy` using `--set-secrets` with correct `dev-nexus-prod_` prefix. Terraform state reconciliation is deferred.

**Deploy workflow — cannot mix env vars and secrets:** Cloud Run already had secrets set via Secret Manager refs. Tried to use `--set-env-vars` → error "Cannot update environment variable to string literal". Switched to `--set-secrets` with correct `dev-nexus-prod_` prefix.

**Deploy workflow — deployments permission:** Added `permissions: deployments: write` to fix "Resource not accessible by integration" on GitHub deployment API.

### Final State
- ✅ Service public: `https://pattern-discovery-agent-75l7mntama-uc.a.run.app` — 200 OK
- ✅ 89 skills registered, version 2.0.0
- ✅ Database: disabled (SQLite — postgres env vars not yet set)
- ✅ Deploy workflow: gcloud-based deploy works
- ⚠️ Terraform state: still out of sync (deferred — separate session)
- ⚠️ PostgreSQL: not connected (postgres VM secrets/env vars not set via deploy)

### Commits Pushed
- `db83a79` fix(ci): clean orphaned state before import to prevent 409 conflicts
- `4826062` fix(ci): set ENV explicitly in scripts, fix empty inputs.environment on push
- `f0c89c7` fix(ci): use gcloud directly for Cloud Run IAM — bypass broken state
- `e39e249` fix(ci): remove invalid 'environment:' job key and duplicate TF_VAR
- `0f2a4c6` fix(ci): force-unlock stale terraform state lock before apply
- `d07f24d` fix(ci): add environment:prod to deploy job to access environment-scoped secrets
- `aa63315` fix(ci): replace Terraform deploy with direct gcloud run deploy
- `40f43d7` fix(ci): use --set-secrets with correct dev-nexus-prod_ prefix
- `56c3d75` fix(ci): add deployments:write permission for GitHub deployment API

### Outstanding Issues (for future sessions)
1. **Terraform state reconciliation** — full `terraform apply` still broken due to state/GCP mismatch
2. **PostgreSQL connection** — postgres VM env vars (host, user, db) not set in deploy
3. **Deploy notification** — Discord webhook may still fail if `DISCORD_WEBHOOK_URL` not set

---

## Previous Session (02:00–02:35 UTC) — elastica project

### Problem: Cloud Run service was 403 Forbidden
User wanted to make `https://pattern-discovery-agent-75l7mntama-uc.a.run.app` public.

### Root Cause Analysis
- `allow_unauthenticated = true` was set in `prod.tfvars` ✅
- `google_cloud_run_service_iam_member.public_access` IAM binding exists in Terraform config ✅
- But: Terraform state in GCS is **severely out of sync** with actual GCP resources
- All postgres, WIF, and Secret Manager resources already exist in GCP but are missing or mis-addressed in Terraform state
- A full `terraform apply` fails with 409 conflicts because it tries to recreate existing resources

### Fixes Applied (3 commits)
1. `fix(ci): clean orphaned state before import to prevent 409 conflicts` — added `terraform state rm` before import step
2. `fix(ci): set ENV explicitly in scripts, fix empty inputs.environment on push` — fixed null ENV variable on push-triggered runs
3. `fix(ci): use gcloud directly for Cloud Run IAM — bypass broken state` — replaced terraform apply with direct `gcloud run services add-iam-policy-binding`

### Final State
- IAM binding applied: `allUsers` + `roles/run.invoker` ✅
- Service went from 403 → 500 ✅ (IAM fixed, but app itself is crashing)
- Terraform workflow: now uses gcloud for IAM, full apply still broken due to state sync

### Backend 500 Error (UNRESOLVED — still needs investigation)
The service is publicly accessible but returning 500 on all requests. Likely causes:
- PostgreSQL connection failing (postgres VM may not be running or accessible)
- VPC connector egress restriction: `PRIVATE_RANGES_ONLY` — but Cloud Run→Postgres traffic is on the internal VPC, so this should work
- Missing env var / secrets not populated
- App startup crash

**Next step:** Check Cloud Run logs in GCP Console for the actual error.

### Commits Pushed
- `db83a79` fix(ci): clean orphaned state before import to prevent 409 conflicts
- `4826062` fix(ci): set ENV explicitly in scripts, fix empty inputs.environment on push
- `f0c89c7` fix(ci): use gcloud directly for Cloud Run IAM — bypass broken state

---

## Previous Session (02:00–02:35 UTC) — elastica project
See memory for elastica-cloudbuild session. Not related to current work.
# 2026-04-03

## resume-customizer — Skills Page Fix Day

### What happened
Multiple attempts to capture a screenshot of the Skills Review page via Playwright UI navigation — all failed. The root issue: Playwright's `fill()` on the job description textarea (`aria-label="Or paste job description manually"`) sets the DOM value but Streamlit's reactive model doesn't register it as a session_state change. So `st.session_state.job_description` stays empty → "Start Workflow" button click does nothing (no spinner, no stage progression).

### Key findings
- **App on port 8501**: resume-customizer Streamlit (confirmed by LLM provider selector showing "Claude Sonnet 4.6")
- **Port 8501 conflict**: Multiple old Streamlit processes accumulate. Kill all before restarting.
- **Orchestrator**: Works correctly with real LLM. `live_llm_test.py` runs end-to-end and produces PDF.
- **Alignment matrix fix** (commit `85f0459`): Already pushed to `origin/main`. `_to_json_serializable()` handles Pydantic models in `alignment_matrix` for the Debug State Info panel's `st.json()` call. App needs restart after pull — which was done.

### What's still unresolved
- Playwright UI navigation for end-to-end tests is broken due to Streamlit textarea reactivity not triggering from Playwright fill
- Screenshots of Skills Review page could not be confirmed via Playwright (all showed Step 1)

### Work done today (pushed)
- `85f0459` — fix: safely serialize alignment_matrix to handle Pydantic models
- Playwright test infra: `conftest.py` → `headless=True`, corrected resume path in `test_v2_e2e_milan.py`
- Screenshot scripts: `fresh_screenshot.py`, `screenshot_skills_review.py`

### Discord channel
`#resume-customizer` (channel id: 1489035741341155408)