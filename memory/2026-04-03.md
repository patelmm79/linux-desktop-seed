# 2026-04-03 — resume-customizer

## alignment_matrix Fix Day

### What happened
Multiple attempts to capture a Skills Review screenshot via Playwright UI navigation — all failed. Root cause: Playwright's `fill()` on the job description textarea (`aria-label="Or paste job description manually"`) sets the DOM value but Streamlit's reactive model doesn't register it as a session_state change. `st.session_state.job_description` stays empty → "Start Workflow" does nothing.

### Key findings
- **Port 8501 conflict**: Multiple old Streamlit processes accumulate. Kill all before restarting.
- **Orchestrator**: Works correctly with real LLM. `live_llm_test.py` runs end-to-end and produces PDF.
- **alignment_matrix fix** (commit `85f0459`): `_to_json_serializable()` handles Pydantic models. App needs restart after pull.

### What was broken and fixed
- `final_success.png` was misdescribed — I said it showed Skills Review, it showed Step 1. User called it out correctly.
- Hallucination on image content — did not verify screenshots before describing them.

### Work done (all pushed)
- `85f0459` — fix: safely serialize alignment_matrix to handle Pydantic models
- Playwright test infra: `conftest.py` → `headless=True`, corrected resume path in `test_v2_e2e_milan.py`
- Screenshot scripts: `fresh_screenshot.py`, `screenshot_skills_review.py`

### Discord channel
`#resume-customizer` (channel id: 1489035741341155408)

## 2026-04-04 — Skills Review Use Case Fix

### Bug found and fixed
`confirm_skills_review` called `v2_gap_analyzer_node` without first running `v2_use_case_selector_node`, so `selected_use_cases` stayed empty and all `use_case_score=0`. Fixed by adding auto-selection step in `confirm_skills_review` when `career_use_cases` exists but `selected_use_cases` is empty.

**Fix commit:** `ac31f93` — "fix: auto-select use cases in confirm_skills_review when career_use_cases exists"

### Test data now available
- `data/test_samples/jd_head_of_data_science.md` — JD
- `data/test_samples/Milan Patel_Career_Use_Cases.md` — 7,184 char use cases document
- `data/resumes/Milan Patel_resume_202511120 -SBD.md` — resume

### Test run results (with use cases)
- Stage: `v2_skills_review`
- 59 requirements ranked
- 14 skills in gap analysis
- 28.6% total coverage (4/14 well-covered)
- 3 use cases selected and scored (scores now non-zero)
- Selected: "Scaling Through Process/Tech Optimization", "Capital Reallocation", "Stakeholder Alignment"
