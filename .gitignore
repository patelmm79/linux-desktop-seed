# Worktrees
.worktrees/
worktrees/

# Local Claude settings (contains test server IP)
.claude/settings.local.json

# Local test scripts (contains sensitive server info)
tests/test-usability.sh

# Environment files (never commit secrets)
.env
.env.local
.env.*.local
*.env

# OpenCLAW runtime configs (contain live tokens)
# On VM: ~/.openclaw/openclaw.json
# Template: config/openclaw-defaults.json
config/openclaw-ideal-config.json
scripts/openclaw-ideal-config.json

# Sensitive runtime configs and scripts
scripts/ccr-final.json
scripts/ccr-config.json
scripts/discord-claude-bridge.sh
scripts/openclaw-working-config.json